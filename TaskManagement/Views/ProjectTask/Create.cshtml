@model TaskManagement.Models.ProjectTask

<h2>Create Task</h2>

<form method="post" action="/ProjectTask/Create" enctype="multipart/form-data">
    @Html.HiddenFor(m => m.ProjectId)
    @Html.Label("Title", "Task Title")
    <br />
     @Html.TextBox("Title", null, new { @class = "form-control" })
    <br /><br/>
    @Html.Label("Description", "Task Description")
    <br />
    @Html.TextBox("Description", null, new { @class = "form-control" })
    <br /><br />
    @Html.Label("Deadline", "Deadline")
    @Html.TextBox("Deadline", null, new { @class = "form-control datepicker" })
    <br />
    <button class="btn btn-sm btn-succes" type="submit">Create Task</button>

</form>